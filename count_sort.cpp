#include <iostream>
#include <vector>
using namespace std;

/* count sort algo and its problems

vector<int> countSort(int arr[]){
    int length = sizeof(arr)/sizeof (arr[0]);
    maxnum = max(arr.begin,arr.end);
}
*/

int main(){

    int arr[] = {2,4,3,7,1}
    maxnum = max(arr.begin,arr.end);
    cout<<maxnum;
    return 0;

}